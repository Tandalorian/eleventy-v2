<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <aside class="sidebar-left">
      <div class="logo"><img src="/images/t-logo.svg" alt="Tanner Mardis T Logo"></div>
      <h1>Tanner Mardis</h1>
    </aside>
    <main class="content">
      {% block content %}
        <!-- Page-specific content goes here -->
      {% endblock %}
    </main>
    <nav class="sidebar-right">
      {% for page in navigation %}
        <button class="nav-button" data-page="{{ page.slug }}" data-image="{{ page.image }}">
            <span class="icon"><img src="{{ page.icon | safe }}" alt="icon"></span>
          <span class="label">{{ page.label }}</span>
        </button>
      {% endfor %}
    </nav>
  </div>
  <script src="/js/scripts.js"></script>
</body>
</html>
